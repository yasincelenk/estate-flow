import type { Metadata } from "next";
import { Geist, Geist_Mono, Playfair_Display } from "next/font/google";
import "./globals.css";
import { ResultsProvider } from "@/context/results-context";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

const playfair = Playfair_Display({
  variable: "--font-playfair",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "EstateFlow AI - Turn Listings into Leads Instantly",
  description: "Generate viral social media content for real estate listings. Paste your Zillow or Redfin link and let AI create engaging Instagram captions, LinkedIn posts, and TikTok scripts.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} ${playfair.variable} antialiased`}
      >
        <ResultsProvider>
          {children}
        </ResultsProvider>
      </body>
    </html>
  );
}
